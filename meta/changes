* Target Android 16 (API 36).

1.0 (10)

* Target Android 14 (API 34).

1.0 (9)

* Logging is off by default. Disabling logging removes the log file.

1.0 (8)

* Don't move files if they were modified less than 30 seconds ago.

1.0 (7)

* Fetch only the last 1M of the log file.
* Copy files in 1M chunks (rather than attempting to read whole files into memory).
* Avoid racing with the camera (foreground service).
* Avoid restarting the foreground service if it's already running.
* Avoid racing between the background job and the foreground service.
